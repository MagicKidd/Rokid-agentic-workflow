# Agent Instructions

Use the shared Agentic Coding Workflow installed at:

```text
<path-to-agentic-coding-workflow>
```

Read workflow rules from `en/rules/` or `zh/rules/`, then load focused skills from `en/skills/` or `zh/skills/` by name. The default programming router is `matt-skills-core.mdc`; non-coding design discussion is routed by `design-thinking-tools.mdc`.
