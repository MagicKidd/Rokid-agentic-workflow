---
description: 新任务启动完整规范 - Phase 1-4 上下文采集、设计思考、方向选择、AI 自主执行链、模型分配策略、教训记忆。由 new-task-trigger.mdc 触发后加载。
alwaysApply: false
---

# 新任务启动规范（元规则）

## 核心原则

```text
设计阶段 = 人类驱动（AI 准备上下文，人类决定方向）
实施阶段 = AI 驱动（方案确认后，AI 自主调用 Skills 链条高效推进）
```

本规则覆盖从任务启动到实施交接的完整前期流程。一旦用户确认方案，AI 自动衔接 Superpowers 工作流链条，充分利用已有 Skills 高效执行。

---

## Phase 1: 自动上下文采集（AI 执行，人类等待）

感知到新任务后，**必须**执行以下上下文采集。其中 1.0 为前置环境嗅探，1.1-1.6 应尽可能使用 Task 工具派发 explore 子代理并行搜索。

### 1.0 项目专属外挂加载（Plugin Hook）

```yaml
嗅探路径: .cursor/rules/*-playbook.mdc, .cursor/rules/project-context.mdc, 或项目根目录下的配置说明
目标: 获取本项目的具体架构、模块缩写、专用路径映射、以及必须的插件清单
外挂字段约定:
  - plugin_hook.path_overrides   # 覆写文档/代码/lessons 默认路径
  - plugin_hook.rule_relations   # 注入项目特定规则关系
  - plugin_hook.required_plugins # 校验/安装 Cursor 插件和 MCP 依赖
  - plugin_hook.repo_sources     # 项目/文档/skills 仓库地址
效果: 
  - 如果找到该外挂配置，立即读取，并使用其中的"专属约定"覆写后续 1.2（文档）、1.4（代码）的默认搜索策略。
  - 读取其中的 `required_plugins` / 依赖声明，提示用户或自动安装缺失的 Cursor Plugins / MCP Servers。
  - 提取当前项目特有的规则关系（动态替代下文的"与核心规则的关系"中的动态项）。
  - 实现通用工作流与定制化项目深度的无缝桥接。
```

### 1.1 历史对话搜索

```yaml
搜索路径: agent-transcripts/ 目录
搜索方式: 用任务关键词在 .jsonl 和 .txt 文件中 grep
目标: 找到与当前任务相关的历史对话，提取关键决策和上下文
排序: 按匹配数量降序，取 TOP 3 最相关对话深入读取
输出: 最相关的 3 条对话 UUID + 核心摘要（使用 [标题](uuid) 格式引用）
```

### 1.2 相关文档匹配

```yaml
搜索路径: 项目文档目录（如 docs/, doc/ 等，优先寻找架构/设计类目录）
搜索方式: 按任务关键词匹配文件名和内容
重点目录:
  - 任务描述、排期、设计方案目录
  - 架构设计文档目录
  - 核心功能/模块设计文档目录
输出: 相关文档路径列表 + 每篇的一句话摘要
```

### 1.3 已有规则检查

```yaml
搜索路径: .cursor/rules/*.mdc
目标: 检查是否有该任务的专属业务或技术规则
现有规则清单: 动态读取并列出当前项目的所有 .mdc 文件
输出: 是否有匹配规则，如有则读取其内容作为上下文
```

### 1.4 代码依赖分析

```yaml
搜索路径: 当前项目的源代码目录（如 src/, app/, lib/ 等）
搜索方式: 先识别项目结构，然后按任务涉及的模块名搜索代码文件
重点目录:
  - 根据项目性质自动识别核心业务逻辑目录
  - 根据项目性质自动识别接口/配置目录
  - 测试框架目录（如 tests/, spec/ 等）
输出: 涉及的核心模块、文件路径、关键接口
```

### 1.5 近期变更检查

```bash
git log --oneline -20
git diff --stat HEAD~5
# 目标: 了解近期开发动态，避免冲突或重复工作
```

### 1.6 双层教训记忆检查

```yaml
搜索路径: 
  - 全局: <user_home>/.cursor/skills/_global_lessons.md (跨项目通用教训)
  - 局部: 项目中的 lessons.md (如 docs/lessons.md 或 .cursor/lessons.md，当前项目特有踩坑)
目标: 加载历史教训，避免重复犯错
行为:
  - 检查上述两个文件，如果存在则读取其全部内容
  - 合并为"历史教训"段，在 Phase 2 中呈现（标明是全局还是局部）
  - 如果文件不存在 → 跳过（首次触发时自动创建）
触发记录: 当用户纠正 AI 的错误时，AI 必须向用户确认这是"全局通用法则"还是"本项目专属坑点"，然后追加到对应的 lessons.md 中，格式如下：
```

```markdown
### YYYY-MM-DD: 一句话教训标题

- **场景**: [什么任务中犯的错]
- **错误**: [AI 做了什么错误的事]
- **正确做法**: [应该怎么做]
- **根因**: [为什么会犯这个错]
```

### 采集验证

Phase 1 完成后，AI 必须自检：

- [ ] 外挂加载：查过了，是否有项目级 playbook/context 配置
- [ ] 历史对话：搜过了，结果是 ___ （有/无相关对话）
- [ ] 文档匹配：搜过了，找到 ___ 篇相关文档
- [ ] 规则检查：查过了，是否命中 ___
- [ ] 代码分析：搜过了，涉及 ___ 个模块
- [ ] 近期变更：查过了，最近 ___ 条相关提交
- [ ] 教训记忆：查过了，有 ___ 条相关教训

**任何一项显示"未搜"则必须补充，不得跳过。**

---

## Phase 2: 上下文摘要呈现（AI 呈现，人类审阅）

采集完成后，**必须**以下述结构化格式向用户呈现摘要：

```markdown
## 任务上下文摘要

### 任务理解
> [用一段话复述对任务的理解]

### 背景信息
- [从文档/历史对话中提取的关键背景，带文档链接]

### 已有设计决策
| 文档 | 核心结论 | 状态 |
|------|---------|------|
| [文档路径] | [一句话结论] | [已完成/进行中/待定] |

### 代码依赖
| 模块 | 路径 | 关系 |
|------|------|------|
| [模块名] | [文件路径] | [依赖/被依赖/需修改] |

### 已有规则
- [是否有 .mdc 规则] → [如有，说明其内容]
- [外挂命中情况] → [是否读取到 plugin_hook 以及命中的关键字段]

### 历史对话
- [对话标题](对话UUID) - [核心内容摘要]

### 待确认项
1. [需要用户确认的问题或假设]
```

---

## Phase 3: 需求确认（人类决策）

摘要呈现后，**等待用户确认**：

1. 任务理解是否准确
2. 待确认项逐一澄清
3. 用户决定下一步方向

**AI 在此阶段只回答问题、提供信息，不主动推进。**

> **【强制设计评审提醒】**
> 如果当前任务涉及以下内容：
> - 新的数据库开表、存储方案、字段设计
> - 核心组件选择和配置
> - 新接口（API）的设计
> **AI 必须在此阶段主动提醒用户**："这些设计方案需要遵循已有规范，并且**必须通过人工评审之后才能够开始实现**。请确认是否已通过评审，或是否需要我先帮您输出设计文档草案供评审？"

---

## Phase 4: 方向选择与执行交接

### Phase 4a: 推荐菜单（人类选择）

基于任务性质，向用户推荐：

```text
方案已确认，推荐以下路径：

A) 深度设计 → brainstorming 陪你讨论方案，逐节确认
B) 方案清晰，需要实施计划 → writing-plans 拆解步骤
C) 简单明确 → 直接执行
D) 需要调试/排查 → systematic-debugging 系统化排查
E) 你自己先想 → 我待命

你怎么说？
```

### Phase 4b: 状态转换协议（确认信号定义）

**AI 进入自主执行的条件**：

```text
显式确认（立即执行）:
  - "选 B" / "走 B" / "B" + 无附加条件
  - "开始吧" / "执行" / "go"
  - "就按你说的来"

附条件确认（先记录约束再执行）:
  - "选 B，但先不要动特定模块" → 记录为执行约束
  - "A，重点讨论模型选型" → 记录为 brainstorming 焦点

不算确认（继续等待）:
  - "嗯" / "有道理" / "看看" → 仅表示理解，不是指令
  - "我想想" / "让我考虑" → 明确未决策
```

**临时任务打断协议（防污染机制）**：
当系统处于 Phase 2、3 或 4 等待人类决策时，如果用户输入了与当前启动流程无关的具体任务（如"帮我查个日志"、"解释一下这个函数"）：

1. **绝对不中止主流程**：主代理不得放弃当前的新任务启动上下文。
2. **派发子代理（继承不污染）**：
   - 使用 `Task` 工具派发子代理处理该临时任务。
   - **继承上下文**：在传递给子代理的 `prompt` 中，必须附带当前主任务的一句话背景（如："背景：我们正在执行[任务名]的设计阶段。当前临时任务是：[用户要求]"）。
   - **不污染主线**：要求子代理只返回最核心的结论（不超过 50 字），或者把详细过程写入临时文件。
3. **强制话题拉回**：在同一条回复中，主代理在展示子代理结果后，必须**原样复述**被中断前等待用户的选择题或问题。

*话术模板*：
"我已经派发子代理在后台调查[临时任务]，它的核心结论是：[一句话结论]。
咱们回到主线，刚才的方案你考虑得怎么样了？选 A 还是 B？"

**当不确定时，追问一句**：`"你是确认走路径 X 开始执行，还是还在考虑？"`

**路径 A brainstorming 退出信号**：
- 用户说"方案定了" / "就这样" / "可以开始做了" → 自动进入路径 B
- 用户说"先到这" / "我再想想" → 暂停，记录当前设计进度
- brainstorming 产出设计文档后，AI 主动提示："设计文档已保存，是否进入实施规划（路径 B）？"

**中途暂停协议**：
- 用户说"等一下" / "暂停" / "停" → 立即停止，汇报当前进度
- 用户说"方向不对" → 回退到 Phase 4a 重新选择

### Phase 4c: AI 自主执行链（确认后自动推进）

收到确认信号后，AI **自主衔接工作流链**，无需逐步请示：

```text
用户确认方案
  │
  ├─ 路径 A（深度设计）:
  │   brainstorming → 设计文档 → 用户确认设计 → 进入路径 B
  │
  ├─ 路径 B（实施规划）:
  │   writing-plans 拆解任务
  │     → [如涉及 LLM] 加载 llm-test-standard + prompt-engineering
  │     → [如涉及前端] 加载 frontend-design / ui-ux-pro-max
  │     → test-driven-development 先写测试
  │     → subagent-driven-development 并行执行
  │     → 每批任务后 requesting-code-review 审查
  │     → verification-before-completion 验证
  │     → 完成后 finishing-a-development-branch 收尾
  │
  ├─ 路径 C（直接执行）:
  │   直接编码
  │     → verification-before-completion 验证
  │     → 完成后报告结果
  │
  └─ 路径 D（调试排查）:
  │   systematic-debugging 四阶段调试
  │     → Phase 1 根因调查 → Phase 2 模式分析
  │     → Phase 3 假设验证 → Phase 4 修复实施
  │     → verification-before-completion 验证
```

### AI 自主执行的约束

在自主推进过程中，AI 遵守以下规则：

1. **声称完成前必须有验证证据** — 跑过测试/检查过输出才能说"完成"
2. **遇到阻塞立即报告** — 不猜测、不绕过，停下来问用户
3. **每个重大步骤后简要汇报** — 一句话说明进展，不需要用户回复即可继续
4. **3 次修复失败先重规划** — 不是直接停下，而是先尝试重新拆解方案；如果重规划方案也被否决，再停下交给用户决策
5. **用户附加约束始终生效** — Phase 4b 记录的条件在整个执行链中遵守
6. **非 trivial 改动前自我审视** — 完成复杂实现后，先问自己"有没有更简洁的方式"，再提交审查；简单修改跳过此步
7. **双层教训记录** — 被用户纠正错误后，向用户确认并记录到 `<user_home>/.cursor/skills/_global_lessons.md` (全局) 或项目内的 `lessons.md` (局部)，防止重复犯错。
8. **教训分级归档** — 若在 Phase 1.6 或执行期间发现任一 lessons.md 教训条目超过 20 条，应主动提议合并同类项，或将已内化的旧教训移动至 `lessons_archive.md` 中，保持主记忆精简。

---

## 模型分配策略（成本优化）

### 核心原则

```text
按认知类型分工，而非按贵贱分层：
  T1 推理层（High-Reasoning Model）  = 需要"想"的任务：设计、计划、审查、验证判断
  T2 编码层（Coding Model）           = 需要"写"的任务：编程、测试实现
  T3 理解层（Multimodal Model）       = 需要"读"的任务：文档理解、前端开发
  T4 机械层（Fast Model）             = 需要"跑"的任务：搜索、git、文件操作
```

### 自动降级（AI 执行）

子代理任务中，以下场景 **必须** 使用 `model="fast"`：

- Phase 1 所有上下文搜索（grep、文件匹配、git 命令）
- Phase 4c 中的 git 操作、跑测试命令、文件复制等机械任务

### 手动切换提醒（AI 必须提示用户）

当任务进入以下阶段时，AI **必须** 提醒用户考虑切换模型：

```text
进入大量编码/测试实现阶段时：
  "接下来是密集编码任务，建议切换到 Coding Model 以节省成本。
   完成后回到 High-Reasoning Model 做代码审查。要切换吗？"

进入文档阅读/前端开发阶段时：
  "接下来需要阅读大量文档 / 进行前端开发，建议切换到 Multimodal Model。
   它的长上下文和视觉理解更适合这类任务。要切换吗？"

编码/前端完成，进入审查阶段时：
  "编码阶段完成，接下来进入代码审查和验证。
   建议切回 High-Reasoning Model 做深度判断。要切换吗？"
```

### 模型-任务映射速查

| 任务阶段 | 推荐模型 | 切换方式 | 理由 |
|---------|---------|---------|------|
| Phase 1 上下文采集 | Fast | 自动（子代理） | 搜索匹配，无需推理 |
| Phase 2 摘要生成 | High-Reasoning | 保持主模型 | 综合判断 |
| Phase 3 需求确认 | High-Reasoning | 保持主模型 | 理解模糊意图 |
| Phase 4a 方向选择 | High-Reasoning | 保持主模型 | 架构决策 |
| Phase 4c 计划拆解 | High-Reasoning | 保持主模型 | 需要架构思维 |
| Phase 4c 密集编码 | Coding | **提醒手动切换** | 代码生成效率高 |
| Phase 4c 文档理解 | Multimodal | **提醒手动切换** | 长上下文 + 多模态 |
| Phase 4c 前端开发 | Multimodal | **提醒手动切换** | 视觉理解 + 前端生成 |
| Phase 4c 代码审查 | High-Reasoning | **提醒切回** | 深度判断 |
| Phase 4c 验证判断 | High-Reasoning | **提醒切回** | 结果分析 |
| Phase 4c 跑命令/git | Fast | 自动（子代理） | 机械执行 |

---

## 路径约定

一般推荐的路径约定（如当前项目有特殊规范，则以项目规范为准，覆盖 skills 的默认路径）：

| 文档类型 | 推荐路径 | 命名规范 |
|---------|------|---------|
| 设计文档 | `docs/design/` | `YYYY-MM-DD-<名称>-design.md` |
| 实施计划 | `docs/plans/` | `YYYY-MM-DD-<名称>-plan.md` |
| 测试报告 | `docs/reports/` | `<模块名>测试报告.md` |
| 任务级规则 | `.cursor/rules/` | `<任务名>.mdc` |

---

## 与核心规则的关系

| 规则 | 关系 | 说明 |
|------|------|------|
| `new-task-trigger.mdc` | 上游 | 触发器检测信号后加载本规则 |
| `ai-coding-protocol.mdc` | 行为基线 | 全局编码行为约束（最小变更、先读后写、diff 自审） |
| `[外挂]-playbook.mdc` | 侧载 | 决定本项目特有约束（插件依赖、路径约定等） |
| `learned-conventions.mdc` | Phase 4c 自动加载 | 从代码库学习的编码约定，编码阶段替代手动采样 |
| *(动态加载的业务规则)* | 可被发现 | 由 Phase 1.3 动态检索出的特定任务规则，优先合并 `plugin_hook.rule_relations` |

## Skills 完整索引

### 工作流 Skills（Superpowers 链）

| 场景 | Skill | 阶段 | 驱动方 |
|------|-------|------|--------|
| 深度设计讨论 | `brainstorming` | Phase 4a 路径 A | 人类驱动 |
| 创建实施计划 | `writing-plans` | Phase 4c 路径 B | AI 驱动 |
| 并行任务执行 | `subagent-driven-development` | Phase 4c 实施中 | AI 驱动 |
| 批量计划执行 | `executing-plans` | Phase 4c 实施中 | AI 驱动 |
| 代码审查 | `requesting-code-review` | Phase 4c 每批后 | AI 驱动 |
| 完成验证 | `verification-before-completion` | Phase 4c 声称完成前 | AI 驱动 |
| 分支收尾 | `finishing-a-development-branch` | Phase 4c 全部完成后 | AI 驱动 |
| 系统化调试 | `systematic-debugging` | Phase 4c 路径 D | AI 驱动 |
| 架构设计指导 | `software-architecture` | Phase 4a 涉及架构时 | 人类驱动 |
| 复杂任务文件规划 | `planning-with-files` | Phase 4c 任务 >5 步 | AI 驱动 |
| 并行独立问题 | `dispatching-parallel-agents` | Phase 4c 多独立子问题 | AI 驱动 |

### 领域 Skills（按任务特征自动加载）

AI 在执行链中检测到以下特征时，**自动加载对应 Skill / Rule**：

| 检测特征 | 加载 Skill / Rule | 说明 |
|---------|-----------|------|
| 任务涉及编码/工程架构代码设计/测试/重构 | `learned-conventions.mdc` | 从代码库自动学习的编码约定，替代手动采样 |
| 任务涉及 Prompt 设计/优化 | `prompt-engineering` | 提示词相关工作 |
| 任务涉及 LLM 模块测试 | `llm-test-standard` | 测试框架、模型对比、Token 统计 |
| 任务涉及编码实现 | `test-driven-development` | 先写测试，Red-Green-Refactor |
| 任务涉及前端/UI 页面 | `frontend-design` | 生产级前端界面（HTML/React/Vue） |
| 任务涉及 UI/UX 设计 | `ui-ux-pro-max` | 样式、配色、字体、布局等设计决策 |
| 任务涉及 Excel 生成/编辑 | `xlsx` (document-skills) | 销售材料、测试报告等表格文件 |
| 任务涉及 HTML 原型截图 | `html-screenshot` | 设计稿导出、原型存档 |
| 需要重构/持续改进 | `kaizen` | 避免过度工程、防错设计、迭代改进 |
| 需要新项目/模块文档 | `project-documentation` | README、开发指南、架构文档 |

### 设计增强 Skills（Phase 3/4a 中按需调用）

| 场景 | Skill | 触发条件 |
|------|-------|---------|
| 设计方案存在分歧 | `expert-debate` | Phase 3 中用户/AI 有不同方案时 |
| 需要多领域视角 | `expert-collaboration` | 任务跨产品+技术+用户体验等多领域 |
| 需要深层理解 | `internalized-cognition` | 需要"成为系统"理解复杂交互 |

---

## 示例场景（通用演练）

### 场景 1: 用户说"开始做商品详情页的重构"

```text
Phase 1 采集（AI 并行执行）:
  - 外挂嗅探: 检测到 .cursor/rules/ecommerce-playbook.mdc，读取项目路径和组件约束
  - 历史对话: 搜索"商品详情" → 找到 4 条相关对话
  - 相关文档: docs/design/商品详情页_设计方案.md §2.1
  - 已有规则: p1-frontend-components.mdc 命中
  - 代码依赖: src/components/Product/ 中相关模块
  - 近期变更: git log 中的相关提交

Phase 2 摘要（AI 呈现）:
  → 结构化摘要，引用设计方案 §2.1

Phase 3 确认（人类决策）:
  → 用户确认："先做核心购买按钮区"

Phase 4a（人类选择）: 路径 A - 需要深度设计
Phase 4b（状态转换）: 用户说"A，先讨论按钮状态逻辑" → 附条件确认
Phase 4c（AI 推进 brainstorming，人类驱动讨论）:
  → brainstorming 讨论无库存、预售、普通购买三种状态
  → 检测到前端任务 → 自动加载 frontend-design + ui-ux-pro-max
  → 用户说"方案定了" → brainstorming 退出信号
  → AI 提示"进入路径 B？" → 用户确认 → 进入 4c 实施链
  → 加载 p1-frontend-components.mdc 规则执行
```

### 场景 2: 用户说"继续上次的支付模块调试"

```text
Phase 1 采集（AI 并行执行）:
  - 历史对话: 找到上次 Webhook 签名验证的排查进度
  - 已有规则: payment-security.mdc 命中

Phase 2 摘要（AI 呈现）:
  → 上次进度、未完成项、测试结论

Phase 3 确认（人类决策）:
  → 用户确认："从签名的 401 报错继续"

Phase 4a（人类选择）: 路径 D - 调试排查
Phase 4b（状态转换）: 用户说"D，按上次说的再查" → 确认信号
Phase 4c（AI 自主推进）:
  → 加载 payment-security.mdc
  → systematic-debugging 四阶段排查
  → verification-before-completion 验证结果
  → 汇报测试结论
```

### 场景 3: 用户说"新增一个报表导出功能"

```text
Phase 1-3（人类驱动）:
  → 采集数据结构设计文档、现有报表代码结构
  → 用户确认需求和约束

Phase 4a（人类选择）: 路径 B - 方案清晰，需要实施计划
Phase 4b（状态转换）: 用户说"选 B，开始" → 确认信号
Phase 4c（AI 全链条自主推进）:
  → writing-plans 拆解为 8 个 bite-sized 任务
  → 检测到涉及 Excel 生成/编辑 → 自动加载 xlsx skill
  → test-driven-development 先写生成逻辑测试
  → subagent-driven-development 并行执行
  → 每 3 个任务后 requesting-code-review
  → verification-before-completion 全量测试
  → finishing-a-development-branch 提交/PR
```