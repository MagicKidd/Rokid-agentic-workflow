---
description: Agent 连续性协议：轻量会话检查、任务账本和防止陈旧文件覆盖
alwaysApply: true
---

# Agent Continuity Protocol

用于让长任务和多 agent 协作始终落在可追踪的项目状态上。

## 新会话检查

每次在项目中开启新会话时：

1. 读取项目本地 agent 指令：`AGENTS.md`、`CLAUDE.md`、`.cursor/rules/*.mdc`。
2. 用 `git status --short` 确认仓库状态。
3. 查看是否存在近期任务账本。
4. 如果是延续任务，先阅读对应账本。
5. 本会话首次编辑目标文件前，从磁盘重新读取最新内容。

## 任务账本

当任务长期、设计重、跨文件/模块、可能多人协作，或用户明确要求记录过程时，创建或更新任务账本。

账本只记录耐久事实：目标、当前状态、已确认事实、决策、触碰文件、验证、未完成事项和交接备注。

## 新鲜度规则

默认轻量：新会话刷新一次状态，首次编辑目标文件前重新读取。

在以下情况升级为严格检查：同一目标文件被别人改动、任务高风险、git status 出现意外变化，或用户要求严格新鲜度。

严格模式下禁止基于陈旧记忆写文件，必须先对齐最新内容。
