---
description: 跨项目动态学习型 AI 编程元规则（最小变更、先读后写、diff 自审）
globs: *
alwaysApply: true
---

# Dynamic AI Coding Protocol（全局行为基线）

## 目标

提供一套可移植的 AI 编码行为协议，适用于任何项目：

- 先学习本地约定再动手；
- 用最小改动解决问题；
- 保证代码可被人长期维护。

## 硬性规则（不可违背）

1. **最小有效变更**
   - 用最小 diff 达成目标。
   - 未被明确要求时，不做无关重构、不做“顺手整理”。

2. **先读后写**
   - 修改模块前先读同目录 2-3 个相邻文件，学习局部风格与惯例。

3. **先复用再创建**
   - 创建新文件/抽象前，先搜索是否已有工具、模式、服务层接口。

4. **Diff 自审**
   - 声称完成前必须检查 diff，移除无关改动。

5. **可维护性优先**
   - 命名清晰、逻辑直白。
   - 注释只解释“为什么”，不写显而易见的“做什么”。

6. **安全基线**
   - 禁止硬编码 secret / key / token / 凭证。
   - 不提交敏感文件（如 `.env`、密钥、证书）除非用户明确要求。

## Read-Before-Write 协议（可执行）

写代码或改代码时按以下顺序执行：

1. **先检查已学习约定**
   - 如果项目存在 `learned-conventions.mdc` 或 `.agent-context/conventions.md`，优先使用其中的提炼结果，**跳过手动采样推断**。

2. **Sample（仅当没有已学习约定时）**
   - 读取目标模块附近 2-3 个文件。

3. **Extract（仅当没有已学习约定时）**
   - 提取命名、导入、类型密度、错误处理、日志、测试风格等约定。

4. **Align**
   - 按“出现频率优先、最近提交次之”的原则对齐风格。

5. **Verify**
   - 对照采样代码做一致性检查，修正明显不一致。

## 反“糊代码”护栏（Anti-Slop）

- 不新增只有一个实现的抽象层。
- 不重复做多层防御式校验（同一输入不要层层重复校验）。
- 不写“无行为增益”的工具包装。
- 不做大范围重写；能一处补丁解决就一处补丁解决。

## 跨编辑器提示

如果项目提供 `AGENTS.md` / `CLAUDE.md` / `.agent-context/*` 或 `learned-conventions.mdc`，应将它们视为**项目本地约定**的权威来源；本文件仅提供行为基线。

