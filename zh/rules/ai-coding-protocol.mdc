---
description: 与 Matt workflow 兼容的全局编码护栏：最小改动、先读后写、验证后收尾
alwaysApply: true
---

# AI Coding Protocol

本文件是轻量护栏。工作流路由由 `matt-skills-core.mdc` 承担；项目专属约定放在项目的 `AGENTS.md`、`CLAUDE.md`、`CONTEXT.md`、`docs/adr/` 或 `.agent-context/*` 中。

如果某个 Matt skill 定义了明确流程、顺序、反馈循环或术语，优先遵循该 skill；本协议只作为安全约束。

## 不可妥协规则

1. **最小有效改动**：用最小 diff 解决问题；不做无关重构。
2. **先读后写**：编辑前阅读附近代码和项目文档，理解本地风格。
3. **先复用再发明**：新增工具、helper、模块或抽象前，先搜索既有等价物。
4. **行为优先于内部**：测试通过公共接口验证可观察行为，不绑定私有实现。
5. **有证据再收尾**：声明完成前运行相关验证；无法验证时说明原因。
6. **人类可维护优先**：命名清楚、逻辑直白，少做透传包装和投机抽象。
7. **安全底线**：不硬编码密钥；不提交凭据文件；提交前区分本次改动和既有无关改动。

## 本地领域升级

只有明确命中窄域时才加载领域插件，例如：`safe-commit`、`community-solution-research`、`design-impl-audit`、`dynamic-agent-context`、`mergeable-feature-dev`、`prompt-engineering`、`frontend-design`。
