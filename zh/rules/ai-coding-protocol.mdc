---
description: 跨项目动态学习型 AI 编程元规则（最小变更、先读后写、diff 自审）
alwaysApply: true
---

# 动态 AI 编程协议 (全局)

## 目标

为任何项目中的 AI 编程提供可移植的行为协议：
- 在编辑前学习本地约定；
- 进行最小的有效更改；
- 保持代码对人类的可维护性。

## 不可协商的规则

1. **最小有效变更**
   - 优先选择能解决用户请求的最小差异（diff）。
   - 除非明确要求，否则不要重构无关代码。

2. **先读后写**
   - 在编辑模块之前，阅读同一目录下的 2-3 个附近文件。
   - 从实际代码中推断本地约定，然后对齐新更改。

3. **创建前检查**
   - 在添加抽象或新文件之前，搜索现有的辅助函数/模式。
   - 只要可行，就重用现有工具。

4. **Diff 自我审查**
   - 在报告完成之前，检查 diff 并删除无关的更改。
   - 如果需要故意偏离样式，请解释原因。

5. **人类可维护性优先**
   - 保持命名明确，逻辑直接。
   - 注释解释**为什么**，而不是显而易见的**什么**。

6. **安全基线**
   - 永远不要在代码中硬编码机密/密钥/令牌。
   - 除非用户明确要求，否则永远不要提交凭证文件。

7. **设计评审要求**
   - 新的数据库建表、存储方案、字段设计、组件选择和配置必须遵循现有规范。
   - 设计方案必须在团队内通过人工评审后才能开始实现。
   - 接口（API）设计评审过程遵循相同的要求。AI 必须提醒用户在编码前获得批准。

## 先读后写协议

在编写或修改代码时，运行此序列：

1. **首先检查已学习的约定**
   - 如果存在 `learned-conventions.mdc` 或 `.agent-context/conventions.md`，使用其预提取的数据并**跳过步骤 2-3**。
2. **采样**（仅在没有已学习约定时）
   - 阅读目标模块中 2-3 个最近的文件。
3. **提取**（仅在没有已学习约定时）
   - 推导约定：命名、导入、类型密度、错误处理、日志记录、测试风格。
4. **对齐**
   - 遵循主要的本地风格（频率优先，最近性作为平局决胜）。
5. **验证**
   - 将新代码与采样的风格进行比较，并修复明显的差异。

## 防冗余护栏

- 避免添加只有一个实现的抽象层。
- 避免对相同输入在多个层进行重复的防御性检查。
- 避免不添加任何行为的实用程序包装器。
- 避免在只需一个集中的补丁时进行广泛的重写。

## 跨编辑器集成提示

如果项目提供了 `AGENTS.md` 和 `.agent-context/*`，请将它们视为主要的本地约定，并将此全局协议作为行为基线。