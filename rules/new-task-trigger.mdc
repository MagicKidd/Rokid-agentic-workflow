---
description: 新任务启动触发器 - 检测新任务/新功能/新设计信号后加载完整启动规范执行 Phase 1-4 流程
alwaysApply: true
---

# 新任务启动触发器

## 核心原则

设计阶段 = 人类驱动（AI 准备上下文，人类决定方向）
实施阶段 = AI 驱动（方案确认后，AI 自主调用 Skills 链条高效推进）

## 触发条件

当对话中感知到以下信号时，触发完整启动流程：

- 用户提及：新任务 / 新功能 / 新设计 / 新模块 / 开始做 / 着手 / 启动 / 接着做
- 用户描述了尚未在项目中存在的功能或需求
- 用户引用了 Notion / 文档 / 任务清单中的待办项
- 用户说"继续上次的工作"或类似恢复语境的表达

**不触发**：纯代码 bug 修复（用 `systematic-debugging`）、简单配置调整、单文件编辑、问答咨询。

## 红旗思维（速查）

| 危险想法 | 现实 |
|---------|------|
| "这很简单，不需要采集上下文" | 未检查假设浪费最多时间 |
| "我先看看代码再说" | 先采集上下文，再看代码 |
| "用户说得很清楚了，直接做" | 用户的上下文在脑子里，你的不在 |
| "上次做过类似的，我记得" | 你不记得，去搜历史对话 |
| "就改一个文件的事" | 确认依赖关系后再动手 |
| "先写代码，边做边想" | 先完成 Phase 1-3，再动手 |

## 触发后行为

检测到上述信号后，**立即使用 Read 工具加载完整启动规范**：

按以下优先级加载 `new-task-kickoff.mdc`，然后严格按照其中 Phase 1-4 流程执行：

```text
优先级 1: 本地全局路径
  读取 ~/.cursor/rules/new-task-kickoff.mdc （AI 自行展开 ~ 为真实用户目录）

优先级 2: 环境初始化与恢复（本地不存在时）
  执行环境准备脚本:
    if [ ! -d ~/.cursor/skills ]; then git clone https://github.com/Rokid/agentic-coding-workflow.git ~/.cursor/skills; else cd ~/.cursor/skills && git pull origin main --ff-only 2>/dev/null; fi
    mkdir -p ~/.cursor/rules
    cp ~/.cursor/skills/rules/new-task-kickoff.mdc ~/.cursor/rules/
  依赖与插件检查:
    - AI 自动检查是否缺失必要的 Cursor Plugins / MCP Servers
    - 如果可以用 CLI/脚本 自动安装则自动安装，否则列出清单提醒用户手动安装
  然后重新读取优先级 1 的路径

优先级 3: 简化流程（Git 恢复也失败时）
  1. 搜索历史对话和相关文档
  2. 呈现上下文摘要
  3. 等待用户确认方向
  4. 按确认方案推进
```

> **路径策略**: 触发器和启动规范均为全局规则，不依赖特定项目目录。任何项目均可复用。
> Git 仓库: `https://github.com/Rokid/agentic-coding-workflow.git` → `rules/new-task-kickoff.mdc`
